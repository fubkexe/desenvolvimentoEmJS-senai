let distancia = 300
let distanciaMax = 200
let valor = 0

if (distancia < distanciaMax){
    valor = distancia * 0.5
    console.log("a viagem ficou no valor de " + valor);
    

    
} else {
    valor = distancia * 0.45
    console.log("a viagem ficou no valor de " + valor);
}